name: 1sr-workflow
on: [push]
jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo command
              run: echo "hello world"
            - name: npm command
              run: node -v
